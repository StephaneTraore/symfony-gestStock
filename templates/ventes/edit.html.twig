{% extends 'base.html.twig' %}

{% block title %}Edit Ventes{% endblock %}

{% block body %}
    <h1>Modifier une vente</h1>

    {{ include('ventes/_form.html.twig', {'button_label': 'Modifier'}) }}

   {# <button class=" btn btn-primary mt-2 mb-2"> <a class='text-white' href="{{ path('app_ventes_index') }}">La liste des ventes </a></button> #}

    {{ include('ventes/_delete_form.html.twig') }}
{% endblock %}
