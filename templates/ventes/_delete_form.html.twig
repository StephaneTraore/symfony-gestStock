<form method="post" action="{{ path('app_ventes_delete', {'id': vente.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente.id) }}">
    <button class="btn btn-danger mb-2">Supprimer</button>
</form>
