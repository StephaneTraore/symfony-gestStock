{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h3>Utilisateur</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>

             <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
             <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>

             <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>

             <tr>
                <th>Date de creation</th>
                <td>{{ user.DateCreation ? user.DateCreation|date('Y-m-d') : ''  }}</td>
            </tr>

            <tr>
                <th>Date de Naissance</th>
                <td>{{ user.dateNaissance ? user.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>

        </tbody>
    </table>

   <button class="btn btn-primary mb-2"> <a class="text-white" href="{{ path('app_user_index') }}">liste des utilisateurs</a> </button>

   <button class="btn btn-info mr-2 mb-2"> <a class="text-white" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a></button>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
