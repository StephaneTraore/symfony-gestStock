{% extends 'base.html.twig' %}

{% block title %}Clients{% endblock %}

{% block body %}
    <h1>Clients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>NomClient</th>
                <td>{{ client.nomClient }}</td>
            </tr>
            <tr>
                <th>PrenomClient</th>
                <td>{{ client.prenomClient }}</td>
            </tr>
            <tr>
                <th>AdresseClient</th>
                <td>{{ client.adresseClient }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ client.Telephone }}</td>
            </tr>
            <tr>
                <th>EmailClient</th>
                <td>{{ client.EmailClient }}</td>
            </tr>
        </tbody>
    </table>

   <button class="btn btn-primary mb-2"> <a  class="text-white" href="{{ path('app_clients_index') }}">La liste des clients</a></button>

   <button class="btn btn-info ml-2 mb-2"> <a class="text-white" href="{{ path('app_clients_edit', {'id': client.id}) }}">Modifier</a></button>

    {{ include('clients/_delete_form.html.twig') }}
{% endblock %}
