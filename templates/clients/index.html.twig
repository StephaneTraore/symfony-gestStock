{% extends 'base.html.twig' %}

{% block title %}Clients index{% endblock %}

{% block body %}
    <h1>Clients index</h1>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomClient</th>
                <th>PrenomClient</th>
                <th>AdresseClient</th>
                <th>Telephone</th>
                <th>EmailClient</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td>{{ client.id }}</td>
                <td>{{ client.nomClient }}</td>
                <td>{{ client.prenomClient }}</td>
                <td>{{ client.adresseClient }}</td>
                <td>{{ client.Telephone }}</td>
                <td>{{ client.EmailClient }}</td>
                <td>
                   <button class="btn btn-primary"> <a  class= "text-white " href="{{ path('app_clients_show', {'id': client.id}) }}">Afficher</a></button>
                   <button class="btn btn-info"> <a class="text-white" href="{{ path('app_clients_edit', {'id': client.id}) }}">Modifier</a></button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Aucun enregistrement</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header">
                <h3 class="card-title">liste des clients</h3>

                <div class="card-tools">
                  <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                    <div class="input-group-append">
                      <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                  <thead>
                    <tr>
                      <th>Id</th>
                      <th>NomClient</th>
                      <th>PrenomClient</th>
                      <th>AdresseClient</th>
                      <th>Telephone</th>
                      <th>EmailClient</th>
                      <th>actions</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr>
                      {% for client in clients %}
            <tr>
                <td>{{ client.id }}</td>
                <td>{{ client.nomClient }}</td>
                <td>{{ client.prenomClient }}</td>
                <td>{{ client.adresseClient }}</td>
                <td>{{ client.Telephone }}</td>
                <td>{{ client.EmailClient }}</td>
                <td>
                   <button class="btn btn-primary"> <a  class= "text-white " href="{{ path('app_clients_show', {'id': client.id}) }}">Afficher</a></button>
                   <button class="btn btn-info"> <a class="text-white" href="{{ path('app_clients_edit', {'id': client.id}) }}">Modifier</a></button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Aucun enregistrement</td>
            </tr>
        {% endfor %}
                    </tr>
                  </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
        </div>

    <button class="btn btn-primary mb-2"><a class="text-white" href="{{ path('app_clients_new') }}">Ajouter un nouveau client </a>
{% endblock %}
