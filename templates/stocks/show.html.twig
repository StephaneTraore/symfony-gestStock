{% extends 'base.html.twig' %}

{% block title %}Stocks{% endblock %}

{% block body %}
    <h1>Stocks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ stock.quantite }}</td>
            </tr>
            <tr>
                <th>PrixUnitaire</th>
                <td>{{ stock.prixUnitaire }}</td>
            </tr>
            <tr>
                <th>DateReception</th>
                <td>{{ stock.dateReception ? stock.dateReception|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stocks_index') }}">back to list</a>

    <a href="{{ path('app_stocks_edit', {'id': stock.id}) }}">edit</a>

    {{ include('stocks/_delete_form.html.twig') }}
{% endblock %}
