<form method="post" action="{{ path('app_stocks_delete', {'id': stock.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
